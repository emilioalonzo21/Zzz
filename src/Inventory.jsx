//inventory page that is loaded once user logins
import React from 'react'

export const Inventory = () => {


    return (
        //htmlFor is used for labels to link them with their inputs, while using the input id
        <div className='inventoryPage'>
            <list className='invPage'>

            </list>
            <button> hello </button>
        </div>
    )
}

// onClick={() => props.offOnForm('register')}>Not registered yet? Register here!